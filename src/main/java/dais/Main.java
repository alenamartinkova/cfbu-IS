package dais;

import dais.entities.Address;
import dais.tables.AddressTable;

import java.sql.SQLException;

public class Main {
    public static void main(String[] args) throws SQLException {
       /* Address a = new Address(35, "BB", "Slovakia", "Moskovska 23");
        AddressTable at = new AddressTable();
        at.insert(a.getId(), a.getCity(), a.getCountry(), a.getAddressLine()); */
    }
}